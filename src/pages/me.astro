---
import { Icon } from "astro-icon/components";
import MainGridLayout from "../layouts/MainGridLayout.astro";
import { getSortedPosts } from "../utils/content-utils";

const author = {
  name: "钟神秀",
  avatar: "https://img-zsx.oss-cn-shanghai.aliyuncs.com/cb04a9a64f09bd4ba85d9908439653ed.jpg",
  email: "mcyzsx815@qq.com",
};

const allBlogPosts = await getSortedPosts();
const totalPosts = allBlogPosts.length;

const startDate = new Date("2025-11-01");
const today = new Date();
const runningDays = Math.floor(
  (today.getTime() - startDate.getTime()) / (1000 * 60 * 60 * 24),
);

/* ===== Busuanzi：无需构建时拉取 ===== */
function formatNumber(n: number) {
  return n >= 10000 ? (n / 10000).toFixed(1) + "万" : n.toString();
}
---

<MainGridLayout>
  <div class="about-page">
    <div class="mobile-only">
      <a href="/" class="back-link">← 返回首页</a>
    </div>

    <div class="about-content">
      <header class="about-header">
        <div class="left-content">
          <h1>关于我</h1>
          <p>总有些事情比永恒更重要！</p>
        </div>
        <div class="right-content">
          <div class="avatar-frame">
            <img src={author.avatar} alt="作者头像" class="avatar-image" />
          </div>
        </div>
      </header>

      <div class="cards-grid">
        <!-- 个人介绍卡片 -->
        <div class="card intro-card">
          <p>您好，很高兴认识您！👋</p>
          <h2>我叫 {author.name}</h2>
          <p>是一名 学生、博客写作者、资源分享者、小说爱好者、博主。</p>
          <Icon name="ph:user-circle-bold" class="card-bg-icon" />
        </div>

        <!-- 信息卡片 - 出生和年龄 -->
        <div class="card info-card age-card">
          <div class="info-item special-info-item">
            <span class="label">出生</span>
            <span class="value">2005</span>
          </div>
          <div class="info-item special-info-item">
            <span class="label">当前</span>
            <span class="value">20岁 <Icon name="ph:graduation-cap-bold" /></span>
          </div>
          <Icon name="ph:calendar-dots-bold" class="card-bg-icon" />
        </div>

        <!-- 座右铭卡片 -->
        <div class="card motto-card">
          <span class="label">座右铭</span>
          <p>造化钟神秀，</p>
          <p>阴阳割昏晓。</p>
          <Icon name="ph:compass-bold" class="card-bg-icon" />
        </div>

        <!-- 关注偏好卡片 -->
        <div class="card tech-card">
          <span class="label">关注偏好</span>
          <h3>资源分享</h3>
          <p>小说、PC游戏</p>
          <Icon name="ph:star-bold" class="card-bg-icon" />
        </div>

        <!-- 音乐偏好卡片 -->
        <div class="card music-card">
          <span class="label">音乐偏好</span>
          <h3>古风、国风等等</h3>
          <p>我喜欢就听</p>
          <Icon name="ph:music-notes-simple-bold" class="card-bg-icon" />
        </div>

        <!-- 性格卡片 -->
        <div class="card info-card personality-card">
          <span class="label">性格</span>
          <div class="content-center">
            <span class="value">提倡者</span>
            <span class="value-small">INFJ-T</span>
          </div>
          <a class="card-link" href="https://www.16personalities.com">在 16 Personalities 了解更多</a>
          <Icon name="ph:brain-bold" class="card-bg-icon" />
        </div>

        <!-- 特长卡片 -->
        <div class="card specialty-card">
          <span class="label">特长</span>
          <p class="specialty-text">活到老，学到老~</p>
          <p class="specialty-text">
            摆烂能力 <span class="highlight">MAX</span>
          </p>
          <Icon name="ph:magic-wand-bold" class="card-bg-icon" />
        </div>

        <!-- 联系方式卡片 -->
        <div class="card contact-card">
          <span class="label">联系我</span>
          <div class="contact-links">
            <a class="contact-link" href={`mailto:${author.email}`} title="邮箱">
              <Icon name="ph:envelope-simple-bold" />
            </a>
            <a class="contact-link" href="https://qm.qq.com/q/QKiovnkOkO" title="微信">
              <Icon name="ri:qq-line" />
            </a>
            <a class="contact-link" href="https://space.bilibili.com/396923563" title="哔哩哔哩">
              <Icon name="ri:bilibili-fill" />
            </a>
            <a class="contact-link" href="https://t.me/zhzsx_me" title="Telegram">
              <Icon name="ph:telegram-logo-bold" />
            </a>
            <a class="contact-link" href="https://x.com/kemiao" title="X">
              <Icon name="ph:x-logo-bold" />
            </a>
          </div>
          <Icon name="ph:address-book-bold" class="card-bg-icon" />
        </div>

        <!-- 网站统计卡片（Busuanzi 版） -->
        <div class="card stats-card">
          <span class="label">网站统计</span>
          <div class="stats-content">
            <div class="stats-grid">
              <div class="stat-item">
                <span class="stat-value">{totalPosts}</span>
                <span class="stat-label">文章数</span>
              </div>
              <div class="stat-item">
                <span class="stat-value">{runningDays}</span>
                <span class="stat-label">运行天数</span>
              </div>
              <div class="stat-item">
                <span class="stat-value" id="busuanzi_today_pv"></span>
                <span class="stat-label">总访客</span>
              </div>
              <div class="stat-item">
                <span class="stat-value" id="busuanzi_today_uv"></span>
                <span class="stat-label">总浏览</span>
              </div>
            </div>
          </div>
          <Icon name="ph:chart-line-bold" class="card-bg-icon" />
        </div>
      </div>
    </div>
  </div>
</MainGridLayout>

<!-- 不蒜子统计脚本 -->
<script src="//cdn.busuanzi.cc/busuanzi/3.6.9/busuanzi.min.js" defer></script>

<style>
  /* 与原文件完全一致，直接复用 */
  .about-page {
    padding: 2rem 1rem;
    min-height: calc(100vh - var(--header-height));
    animation: float-in 0.3s backwards;
  }
  .about-content {
    max-width: 1000px;
    margin: 0 auto;
  }
  .mobile-only {
    display: none;
    margin-bottom: 1rem;
  }
  .back-link {
    display: inline-flex;
    align-items: center;
    color: var(--c-primary);
    text-decoration: none;
    font-size: 1rem;
  }
  .back-link:hover {
    text-decoration: underline;
  }
  .about-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 3rem;
    padding: 1rem 0;
    text-align: left;
  }
  .left-content h1 {
    margin-bottom: 0.5rem;
    font-size: 2.5rem;
    font-weight: 800;
    color: var(--c-text-1);
  }
  .left-content p {
    margin: 0;
    font-size: 1.2rem;
    color: var(--c-text-2);
  }
  .right-content {
    display: flex;
    align-items: center;
    justify-content: center;
  }
  .avatar-frame {
    display: flex;
    flex-shrink: 0;
    align-items: center;
    justify-content: center;
    overflow: hidden;
    width: 120px;
    height: 120px;
    border: 3px solid var(--c-border);
    border-radius: 50%;
    background-color: var(--c-bg-soft);
    transition: all 0.3s ease;
  }
  .avatar-image {
    display: block;
    width: 100%;
    height: 100%;
    object-fit: cover;
  }
  .cards-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 1.5rem;
  }
  .card {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    position: relative;
    overflow: hidden;
    min-height: 220px;
    padding: 2rem 1.5rem;
    border: 1px solid var(--c-border);
    border-radius: 1.5rem;
    background-color: var(--ld-bg-card);
    text-align: center;
    transition: all 0.3s ease;
    box-shadow: none;
  }
  .card:hover {
    box-shadow: none;
    transform: none;
  }
  .card .label {
    position: absolute;
    opacity: 0.8;
    top: 1rem;
    left: 1.5rem;
    margin: 0;
    font-size: 0.8rem;
    color: var(--c-text-2);
  }
  .card-bg-icon {
    position: absolute;
    opacity: 0.1;
    right: 1rem;
    bottom: 1rem;
    font-size: 5rem;
    color: var(--c-text-1);
    pointer-events: none;
  }
  .intro-card {
    grid-column: 1 / -1;
    color: var(--c-text-1);
  }
  .intro-card h2 {
    margin: 0.5rem 0;
    font-size: 3rem;
    font-weight: bold;
  }
  .info-card {
    align-items: stretch;
    justify-content: center;
    padding: 2.5rem 1.5rem;
    color: var(--c-text-1);
  }
  .info-item {
    display: flex;
    flex-direction: column;
    flex-grow: 1;
    align-items: flex-start;
    justify-content: center;
    position: relative;
    width: 100%;
  }
  .info-item .label {
    flex-shrink: 0;
    position: static;
    width: 100%;
    margin-bottom: 0.5rem;
    text-align: left;
  }
  .value {
    display: block;
    width: 100%;
    font-size: 2.5rem;
    font-weight: bold;
    text-align: center;
  }
  .value-small {
    display: block;
    width: 100%;
    font-size: 2rem;
    font-weight: bold;
    text-align: center;
  }
  .card-link {
    position: absolute;
    right: 1.5rem;
    bottom: 1rem;
    font-size: 0.8rem;
    color: var(--c-text-2);
  }
  .card-link:hover {
    color: var(--c-primary);
  }
  .age-card {
    padding: 0.4rem 1.5rem 0.5rem;
  }
  .motto-card {
    color: var(--c-text-1);
  }
  .motto-card p {
    margin: 0;
    font-size: 2.5rem;
    font-weight: bold;
    line-height: 1.2;
  }
  .tech-card {
    color: var(--c-text-1);
  }
  .tech-card h3 {
    margin: 0.5rem 0;
    font-size: 3rem;
    font-weight: bold;
  }
  .tech-card p {
    color: var(--c-text-2);
  }
  .music-card {
    color: var(--c-text-1);
  }
  .music-card h3 {
    font-size: 2.5rem;
    font-weight: bold;
  }
  .music-card p {
    color: var(--c-text-2);
  }
  .specialty-card {
    font-size: 1.8rem;
    font-weight: bold;
    text-align: center;
    color: var(--c-text-1);
  }
  .specialty-text {
    margin: 0.2em 0;
  }
  .highlight {
    display: inline-block;
    font-size: 2.5rem;
    line-height: 1;
    color: var(--c-primary);
  }
  .contact-card {
    grid-column: 1 / -1;
    color: var(--c-text-1);
  }
  .contact-links {
    display: flex;
    justify-content: center;
    flex-wrap: wrap;
    max-width: 600px;
    margin: 0 auto;
  }
  .contact-link {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 50px;
    height: 50px;
    background-color: var(--c-bg-1);
    border: 1px solid var(--c-border);
    border-radius: 50%;
    color: var(--c-text-1);
    font-size: 1.4rem;
    transition: all 0.2s ease;
    padding: 0;
    box-shadow: none;
    text-decoration: none;
  }
  .contact-link:hover {
    background-color: var(--c-bg-soft);
    color: var(--c-text);
  }
  .stats-card {
    grid-column: 1 / -1;
    color: var(--c-text-1);
  }
  .stats-content {
    width: 100%;
  }
  .stats-range-section {
    margin-bottom: 0;
  }
  .stats-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(120px, 1fr));
    gap: 1rem;
    margin-bottom: 0;
  }
  .stat-item {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    padding: 0.5rem;
    background-color: var(--c-bg-1);
    border-radius: 0.8rem;
    transition: transform 0.2s ease;
  }
  .stat-value {
    margin-bottom: 0.25rem;
    font-size: 2rem;
    font-weight: bold;
    color: var(--c-text-1);
  }
  .stat-label {
    opacity: 0.9;
    font-size: 0.9rem;
    color: var(--c-text-2);
  }
  @keyframes float-in {
    from {
      opacity: 0;
      transform: translateY(10px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
  @media (max-width: 768px) {
    .mobile-only {
      display: block;
    }
    .about-page {
      padding: 1rem;
    }
    .about-header {
      flex-direction: column;
      text-align: center;
      margin-bottom: 2rem;
    }
    .about-header .left-content {
      margin-bottom: 1.5rem;
    }
    .avatar-frame {
      width: 100px;
      height: 100px;
    }
    .card {
      padding: 1.5rem 1rem;
    }
    .cards-grid {
      gap: 1rem;
    }
    .age-card {
      padding: 0.4rem 1.5rem 0.5rem;
    }
    .contact-card .contact-link {
      width: 45px;
      height: 45px;
      font-size: 1.2rem;
    }
    .stats-card {
      padding: 3rem 1rem 1.5rem;
    }
    .stats-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }
  :root {
    --c-text-1: #1a1a1a;
    --c-text-2: #666666;
    --c-border: #e5e5e5;
    --c-bg-soft: #f5f5f5;
    --c-bg-1: #ffffff;
    --c-bg-2: #fafafa;
    --c-primary: #007acc;
    --ld-bg-card: #ffffff;
  }
  html.dark {
    --c-text-1: #e5e5e5;
    --c-text-2: #999999;
    --c-border: #333333;
    --c-bg-soft: #1a1a1a;
    --c-bg-1: #0d0d0d;
    --c-bg-2: #1a1a1a;
    --c-primary: #66b3ff;
    --ld-bg-card: #1a1a1a;
  }
  html.dark .card {
    border-color: var(--c-border);
    background-color: var(--c-bg-2);
  }
  html.dark .label,
  html.dark .card-bg-icon,
  html.dark .tech-card p,
  html.dark .stat-label {
    color: var(--c-text-2);
  }
  html.dark .info-card .card-link {
    color: var(--c-text-2);
  }
  html.dark .info-card .card-link:hover {
    color: var(--c-primary);
  }
  html.dark .specialty-card .highlight {
    color: var(--c-primary);
  }
  html.dark .contact-card .contact-link {
    background-color: var(--c-bg-1);
    border-color: var(--c-border);
    color: var(--c-text-1);
  }
  html.dark .contact-card .contact-link:hover {
    background-color: var(--c-bg-soft);
    color: var(--c-text);
    border-color: var(--c-border);
  }
  html.dark .stat-item {
    background-color: var(--c-bg-1);
  }
  html.dark .avatar-frame {
    border-color: var(--c-border);
    background-color: var(--c-bg-soft);
  }
  html.dark .card svg,
  html.dark .card .card-bg-icon {
    filter: brightness(1.5) contrast(1.2);
  }
</style>